spring:
  application:
    name: api-core
  datasource:
    url: jdbc:h2:file:./data/apiweaver
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  h2:
    console:
      enabled: true
