spring:
  application:
    name: api-core
  datasource:
    url: jdbc:h2:mem:apiweaver-test-${random.uuid};DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  h2:
    console:
      enabled: false
